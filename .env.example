# URL del API backend (solo servidor; incluye /api). No expone el JWT al navegador.
API_URL=http://localhost:3010/api

# Opcional: respaldo si ya usabas NEXT_PUBLIC_API_URL en despliegues anteriores.
# NEXT_PUBLIC_API_URL=http://localhost:3010/api
